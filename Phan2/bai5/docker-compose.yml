version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: redis_container
    ports:
      - "6379:6379" 
    volumes:
      - redis-data:/data # Lưu trữ dữ liệu Redis trên máy host
    restart: always # Khởi động lại container nếu nó bị dừng 
volumes:
  redis-data:

